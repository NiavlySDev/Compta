<UserControl x:Class="BlackWoodsCompta.WPF.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:BlackWoodsCompta.WPF.Helpers"
             Background="#1E1E1E">
    
    <UserControl.Resources>
        <helpers:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="📦" FontSize="36" Margin="0,0,15,0"/>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Inventaire" FontSize="28" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="Gestion du stock des matières premières et plats préparés" FontSize="14" Foreground="#AAAAAA"/>
            </StackPanel>
        </StackPanel>

        <!-- Filters and Controls -->
        <Border Grid.Row="1" Background="#2D2D2D" CornerRadius="10" Padding="20" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="🔍" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                             Width="250" Height="35" Background="#4D4D4D" Foreground="White"
                             BorderBrush="#673AB7" Padding="10" VerticalContentAlignment="Center"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0">
                    <CheckBox IsChecked="{Binding ShowLowStockOnly}" Content="Stock faible" Foreground="White" Margin="0,0,15,0"/>
                    <CheckBox IsChecked="{Binding ShowExpiredOnly}" Content="Expirés/Périssent" Foreground="White"/>
                </StackPanel>
                
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="🔄 Actualiser" Command="{Binding LoadInventoryCommand}"
                            Background="#673AB7" Foreground="White" BorderThickness="0" 
                            Padding="15,8" Margin="0,0,10,0"/>
                    <Button Content="➕ Ajouter" Command="{Binding OpenAddDialogCommand}"
                            Background="#4CAF50" Foreground="White" BorderThickness="0" 
                            Padding="15,8"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Modern Inventory Cards -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding InventoryItems}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#2D2D2D" CornerRadius="12" Margin="10" Padding="25" Width="350" Height="300"
                                BorderBrush="#673AB7" BorderThickness="1">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <!-- Product Header -->
                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                                    <Border Background="#673AB7" CornerRadius="25" Width="50" Height="50" Margin="0,0,15,0">
                                        <TextBlock Text="📦" FontSize="24" HorizontalAlignment="Center" 
                                                   VerticalAlignment="Center" Foreground="White"/>
                                    </Border>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="{Binding ProductName}" FontSize="18" FontWeight="Bold" Foreground="White"/>
                                        <TextBlock Text="{Binding Category}" FontSize="14" Foreground="#673AB7" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </StackPanel>
                                
                                <!-- Stock Status -->
                                <Border Grid.Row="1" Background="#4CAF50" CornerRadius="6" Padding="12,8" 
                                        HorizontalAlignment="Left" Margin="0,0,0,15">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="📊" FontSize="14" Margin="0,0,8,0"/>
                                        <TextBlock Text="{Binding Quantity, StringFormat='{0:F0} unités'}" 
                                                   FontSize="14" FontWeight="Bold" Foreground="White"/>
                                    </StackPanel>
                                </Border>
                                
                                <!-- Details -->
                                <StackPanel Grid.Row="2">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                        <TextBlock Text="💰" FontSize="14" Margin="0,0,8,0"/>
                                        <TextBlock Text="Prix:" FontSize="14" Foreground="#AAAAAA" Margin="0,0,8,0"/>
                                        <TextBlock Text="{Binding UnitCost, StringFormat='{0:C}'}" 
                                                   FontSize="14" Foreground="White" FontWeight="SemiBold"/>
                                    </StackPanel>
                                    
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                        <TextBlock Text="🏪" FontSize="14" Margin="0,0,8,0"/>
                                        <TextBlock Text="Fournisseur:" FontSize="14" Foreground="#AAAAAA" Margin="0,0,8,0"/>
                                        <TextBlock Text="{Binding Supplier}" FontSize="14" Foreground="White"/>
                                    </StackPanel>
                                    
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                        <TextBlock Text="📅" FontSize="14" Margin="0,0,8,0"/>
                                        <TextBlock Text="Expiration:" FontSize="14" Foreground="#AAAAAA" Margin="0,0,8,0"/>
                                        <TextBlock Text="{Binding ExpiryDate, StringFormat='dd/MM/yyyy'}" 
                                                   FontSize="14" Foreground="White"/>
                                    </StackPanel>
                                    
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="⚠️" FontSize="14" Margin="0,0,8,0"/>
                                        <TextBlock Text="Seuil min:" FontSize="14" Foreground="#AAAAAA" Margin="0,0,8,0"/>
                                        <TextBlock Text="{Binding MinQuantity, StringFormat='{0:F0} unités'}" 
                                                   FontSize="14" Foreground="#FF9800"/>
                                    </StackPanel>
                                </StackPanel>
                                
                                <!-- Actions -->
                                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                                    <Button Content="✏️ Modifier" Background="#673AB7" Foreground="White" 
                                            BorderThickness="0" Padding="10,6" Margin="0,0,8,0"/>
                                    <Button Content="🗑️ Supprimer" Background="#D32F2F" Foreground="White" 
                                            BorderThickness="0" Padding="10,6"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Loading Overlay -->
        <Grid Grid.RowSpan="3" Background="#80000000" 
              Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border Background="#2D2D2D" CornerRadius="10" Padding="30" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="⏳" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="Chargement de l'inventaire..." FontSize="16" Foreground="White" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>