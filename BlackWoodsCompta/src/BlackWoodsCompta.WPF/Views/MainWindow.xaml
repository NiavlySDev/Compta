<Window x:Class="BlackWoodsCompta.WPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BlackWoods Compta" 
        Height="800" Width="1400"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Background="#1E1E1E">
    
    <Window.Resources>
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3E3E42"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#673AB7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="#2D2D30">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Border Grid.Row="0" Margin="20" Background="#673AB7" Padding="15" CornerRadius="4">
                    <StackPanel>
                        <TextBlock Text="🏢 BlackWoods" 
                                  FontSize="20" 
                                  FontWeight="Bold"
                                  Foreground="White"
                                  HorizontalAlignment="Center"/>
                        <TextBlock Text="Restaurant" 
                                  FontSize="12" 
                                  Foreground="White"
                              Opacity="0.8"
                              HorizontalAlignment="Center"
                              Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Menu -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">
                        
                        <!-- Dashboard -->
                        <Button Command="{Binding NavigateToDashboardCommand}"
                               Style="{StaticResource MenuButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📊" FontSize="24" Margin="10,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Dashboard" VerticalAlignment="Center" FontSize="14"/>
                            </StackPanel>
                        </Button>

                        <!-- Transactions -->
                        <Button Command="{Binding NavigateToTransactionsCommand}"
                               Style="{StaticResource MenuButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="💵" FontSize="24" Margin="10,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Transactions" VerticalAlignment="Center" FontSize="14"/>
                            </StackPanel>
                        </Button>

                        <!-- Employees -->
                        <Button Command="{Binding NavigateToEmployeesCommand}"
                               Style="{StaticResource MenuButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="👥" FontSize="24" Margin="10,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Employés" VerticalAlignment="Center" FontSize="14"/>
                            </StackPanel>
                        </Button>

                        <!-- Payrolls -->
                        <Button Command="{Binding NavigateToPayrollsCommand}"
                               Style="{StaticResource MenuButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="💰" FontSize="24" Margin="10,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Paies" VerticalAlignment="Center" FontSize="14"/>
                            </StackPanel>
                        </Button>

                        <!-- Inventory -->
                        <Button Command="{Binding NavigateToInventoryCommand}"
                               Style="{StaticResource MenuButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📦" FontSize="24" Margin="10,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Inventaire" VerticalAlignment="Center" FontSize="14"/>
                            </StackPanel>
                        </Button>

                        <!-- Orders -->
                        <Button Command="{Binding NavigateToOrdersCommand}"
                               Style="{StaticResource MenuButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📋" FontSize="24" Margin="10,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Commandes" VerticalAlignment="Center" FontSize="14"/>
                            </StackPanel>
                        </Button>
                        <!-- Suppliers -->
                        <Button Command="{Binding NavigateToSuppliersCommand}"
                               Style="{StaticResource MenuButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🏭" FontSize="24" Margin="10,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Fournisseurs" VerticalAlignment="Center" FontSize="14"/>
                            </StackPanel>
                        </Button>

                        <!-- Reimbursements -->
                        <Button Command="{Binding NavigateToReimbursementsCommand}"
                               Style="{StaticResource MenuButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="💳" FontSize="24" Margin="10,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Remboursements" VerticalAlignment="Center" FontSize="14"/>
                            </StackPanel>
                        </Button>
                        <!-- Invoices -->
                        <Button Command="{Binding NavigateToInvoicesCommand}"
                               Style="{StaticResource MenuButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📄" FontSize="24" Margin="10,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Factures" VerticalAlignment="Center" FontSize="14"/>
                            </StackPanel>
                        </Button>

                        <!-- Cash Register -->
                        <Button Command="{Binding NavigateToCashRegisterCommand}"
                               Style="{StaticResource MenuButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🏪" FontSize="24" Margin="10,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Caisse" VerticalAlignment="Center" FontSize="14"/>
                            </StackPanel>
                        </Button>

                        <!-- Reports -->
                        <Button Command="{Binding NavigateToReportsCommand}"
                               Style="{StaticResource MenuButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📊" FontSize="24" Margin="10,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Rapports" VerticalAlignment="Center" FontSize="14"/>
                            </StackPanel>
                        </Button>

                        <!-- Settings -->
                        <Button Command="{Binding NavigateToSettingsCommand}"
                               Style="{StaticResource MenuButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⚙️" FontSize="24" Margin="10,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Paramètres" VerticalAlignment="Center" FontSize="14"/>
                            </StackPanel>
                        </Button>

                    </StackPanel>
                </ScrollViewer>

                <!-- User Info & Logout -->
                <Border Grid.Row="2" Background="#1E1E1E" Padding="15">
                    <StackPanel>
                        <TextBlock Text="{Binding CurrentUserName}" 
                                  Foreground="White" 
                                  FontWeight="SemiBold"
                                  FontSize="14"/>
                        <TextBlock Text="{Binding CurrentUserRole}" 
                                  Foreground="#AAAAAA"
                                  FontSize="12"
                                  Margin="0,2,0,10"/>
                        <Button Command="{Binding LogoutCommand}"
                               Content="Déconnexion"
                               Background="Transparent"
                               Foreground="White"
                               BorderBrush="White"
                               BorderThickness="1"
                               Padding="10,5"
                               Cursor="Hand">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        Padding="{TemplateBinding Padding}"
                                                        CornerRadius="4">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#673AB7"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Border Grid.Column="1" Background="#1E1E1E" Padding="20">
            <ContentControl Content="{Binding CurrentView}"/>
        </Border>
    </Grid>
</Window>
